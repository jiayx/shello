{
  "name": "shello",
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "dev": "pnpm --filter @shello/web run dev",
    "build": "pnpm --filter @shello/web run build",
    "deploy": "pnpm --filter @shello/web run deploy"
  }
}
